<div>
    <h1>Lista de categorias:</h1>
    <a href="/category/cadastrar"><button class="btn btn-large green">Nova categoria</button></a>
    {{#each categories}}

  <div class="row">
    <div class="col s12 m12">
      <div class="card  white">
        <div class="card-content grey-text">
          <span class="card-title">{{name}}</span>
          <p>Slug{{slug}}</p>
        </div>
      </div>
    </div>
  </div>
       
    {{/each}}
   
</div>